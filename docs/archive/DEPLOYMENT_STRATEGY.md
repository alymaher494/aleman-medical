# دليل نشر موقع الإيمان (Vercel + cPanel)

بناءً على طلبك، قمنا بتنظيم خطة النشر بحيث يتم فصل الواجهة الأمامية عن لوحة التحكم لضمان أفضل أداء وأمان.

## 🏗️ الهيكل المقترح
*   **الواجهة الأمامية (Frontend):** يتم رفعها على **Vercel** (لأنها الأفضل لـ Next.js).
*   **لوحة التحكم (Backend/WordPress):** يتم رفعها على **cPanel** (باستخدام دومين فرعي مثل `admin.al-eman.com`).

---

## 🛠️ الخطوة 1: تجهيز WordPress على cPanel
1.  قم بتثبيت WordPress على دومين فرعي (مثلاً `admin.al-eman.com`).
2.  قم بتثبيت الإضافات التالية وتفعيلها:
    *   **WPGraphQL**: المجلد الأساسي للتواصل.
    *   **Polylang**: لإدارة اللغات.
    *   **WPGraphQL for Polylang**: لتمويل اللغات عبر GraphQL.
3.  تأكد من إعداد الروابط الدائمة (Permalinks) على نظام "Post Name".
4.  قم بتسجيل الـ Custom Post Types (Services, Clients) وتأكد من تفعيل خيار **"Show in GraphQL"** لكل منها.

---

## 🚀 الخطوة 2: نشر الواجهة الأمامية على Vercel
1.  قم برفع الكود إلى مستودع (GitHub / GitLab / Bitbucket).
2.  اربط المستودع بـ Vercel.
3.  **هام جداً:** في إعدادات المشروع على Vercel، أضف متغير البيئة التالي:
    *   Key: `NEXT_PUBLIC_WORDPRESS_API_URL`
    *   Value: `https://admin.al-eman.com/graphql` (رابط الـ API الخاص بموقعك).

---

## 🖼️ الخطوة 3: إعدادات الصور (next.config.ts)
يجب السماح لـ Next.js بجلب الصور من دومين الـ cPanel الخاص بك. قم بتعديل ملف `next.config.ts` بإضافة الدومين الخاص بك:

```typescript
// next.config.ts
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'admin.al-eman.com', // استبدل هذا بدومينك الفعلي
      },
    ],
  },
}
```

---

## 🧪 الخطوة 4: الاختبار النهائي
بعد النشر، يمكنك استخدام صفحة التشخيص للتأكد من أن كل شيء يعمل بسلاسة:
`https://al-eman.com/ar/wp-debug`

---

## ⚠️ ملاحظات هامة
*   **أداء الصور:** بما أن اللوجو ملف SVG بحجم كبير (6MB)، يفضل بشدة ضغطه قبل الرفع أو تحويله لـ PNG لتقليل حجم تحميل الصفحة (Page Load Duration).
*   **التحديث التلقائي:** المشروع مضبوط لإعادة تحديث البيانات كل 60 ثانية (ISR)، مما يعني أن أي تغيير في ووردبريس سيظهر خلال دقيقة واحدة في الموقع دون الحاجة لإعادة بناء الكود.

**نحن جاهزون للبدء في أي مرحلة تختارها!**
